
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@*<!DOCTYPE html>
    <html>
    <head>
        <title>Heading Chart</title>
        <script src="https://www.gstatic.com/charts/loader.js"></script>
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
        <script>
            google.charts.load('current', { packages: ['corechart'] });
            google.charts.setOnLoadCallback(drawChart);

            function drawChart() {
                $.ajax({
                    url: '@Url.Action("HeadingChart", "Chart")',
                    dataType: 'json',
                    success: function (data) {
                        var dataArray = [['Heading Name', 'Heading Count']];
                        for (var i = 0; i < data.length; i++) {
                            dataArray.push([data[i].HeadingName, data[i].HeadingCount]);
                        }
                        var chartData = google.visualization.arrayToDataTable(dataArray);

                        var options = {
                            title: 'Heading Counts',
                            legend: { position: 'bottom' }
                        };

                        var chart = new google.visualization.BarChart(document.getElementById('chart_div'));
                        chart.draw(chartData, options);
                    }
                });
            }
        </script>
    </head>
    <body>
        <div id="chart_div" style="width: 900px; height: 500px;"></div>
    </body>
    </html>*@

@*Pie Chart*@
<!DOCTYPE html>
<html>
<head>
    <title>Heading Chart</title>
    <script src="https://www.gstatic.com/charts/loader.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script>
        google.charts.load('current', { packages: ['corechart'] });
        google.charts.setOnLoadCallback(drawChart);

        function drawChart() {
            $.ajax({
                url: '@Url.Action("HeadingChart", "Chart")',
                dataType: 'json',
                success: function (data) {
                    var dataArray = [['Heading Name', 'Heading Count']];
                    for (var i = 0; i < data.length; i++) {
                        dataArray.push([data[i].HeadingName, data[i].HeadingCount]);
                    }
                    var chartData = google.visualization.arrayToDataTable(dataArray);

                    var options = {
                        title: 'Başlık-Yazı Grafiği',
                        pieHole: 0.4
                    };

                    var chart = new google.visualization.PieChart(document.getElementById('chart_div'));
                    chart.draw(chartData, options);
                }
            });
        }
    </script>
</head>
<body>
    <div id="chart_div" style="width: 1230px; height: 748px;"></div>
</body>
</html>




